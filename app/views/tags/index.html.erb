<h1>All Tags</h1>

<ul id = "tags">
  <% @tags.each do |tag| %>
  <li>><%= link_to tag.name, tag_path(tag) %></li><br />
  <% end %>
</ul><br />
<%= link_to "Create New Tag", new_tag_path, class: "new_tag"%>